# Attack Scripts Directory

This directory is intended to contain Python and Bash scripts that demonstrate the vulnerabilities present in the `vulnerable/` backend.

*Example scripts to add:*
- `sqli_login_bypass.py`
- `brute_force_login.sh`
- `session_hijacking.js`
